/** Max characters for session title before truncation. */
export const TITLE_TRUNCATE_LEN = 80;

/** Number of message groups to show initially in transcript view. */
export const TRANSCRIPT_INITIAL_COUNT = 100;

/** Number of additional groups to load on scroll. */
export const TRANSCRIPT_LOAD_MORE = 80;

/** Max sessions to request from backend. */
export const SESSION_PAGE_SIZE = 200;

/** Max graph nodes before truncation. */
export const GRAPH_MAX_NODES = 300;
